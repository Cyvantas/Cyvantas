/**
 * Temporary Home route — verifies the application shell renders end to end
 * (Navbar, main content, Footer). Real homepage hero and marketing content
 * are built in a later phase.
 */
export function Home() {
  return (
    <div>
      <h1 className="text-display text-foreground">CYVANTAS</h1>
      <p className="text-body mt-4 text-muted">Main application shell</p>
    </div>
  );
}
